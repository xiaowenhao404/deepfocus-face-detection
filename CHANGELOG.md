# DeepFocus 版本更迭报告

## 项目概述

**项目名称**: DeepFocus Pro - 智能人脸定位系统  
**最终版本**: v3.0  
**开发周期**: 2024年12月  
**技术栈**: Python 3.9+ / PyQt5 / OpenCV / dlib / face_recognition

---

## 一、模型选择与迭代

### 1.1 模型演进路线

```
v1.0 基础版
    └── HOG + dlib (基础人脸检测)
         │
v1.x 增强版
    ├── HOG模式 (快速，CPU友好)
    └── CNN模式 (高精度，GPU加速)
         │
v2.x 多引擎版
    ├── HOG模式
    ├── CNN模式
    ├── InsightFace (RetinaFace+ArcFace) ← 弃用
    └── YuNet+SFace (OpenCV DNN) ← 新增
         │
v3.0 最终版
    ├── HOG模式 ✅ 保留
    ├── CNN模式 ✅ 保留
    └── YuNet模式 ✅ 保留
```

### 1.2 各模型技术分析

#### HOG模式 (Histogram of Oriented Gradients)

| 项目 | 说明 |
|------|------|
| **检测器** | HOG特征 + 线性SVM分类器 |
| **识别器** | dlib 128维人脸特征编码 |
| **运行环境** | 纯CPU |
| **优点** | 速度快，资源占用低，适合实时场景 |
| **缺点** | 对侧脸、遮挡、小脸检测能力较弱 |
| **适用场景** | 近距离正脸、实时性要求高 |

**优化措施**:
- v2.1: 针对大图（>1500px）自动降低上采样次数
- v2.2: 大图预缩放到1200px再检测，提高速度

#### CNN模式 (MMOD CNN)

| 项目 | 说明 |
|------|------|
| **检测器** | Max-Margin Object Detection CNN |
| **识别器** | dlib 128维人脸特征编码 |
| **运行环境** | CPU / CUDA GPU |
| **优点** | 高精度，对侧脸、遮挡、小脸检测好 |
| **缺点** | 速度慢，CPU模式尤其明显 |
| **适用场景** | 复杂场景、侧脸检测、有GPU加速时 |

**优化措施**:
- v2.1: 自动检测CUDA可用性，优先使用GPU
- v2.2: 根据图像尺寸自动调整上采样次数
- v2.2: 超大图（>2000px）预缩放优化

#### InsightFace (已弃用)

| 项目 | 说明 |
|------|------|
| **检测器** | RetinaFace |
| **识别器** | ArcFace (512维特征) |
| **运行环境** | ONNX Runtime (CPU/GPU) |
| **理论优点** | 业界顶级精度，对极端姿态鲁棒 |

**弃用原因**:
1. **库冲突严重**: InsightFace依赖的 `onnxruntime` 与 PyTorch/dlib 的 CUDA 环境存在 DLL 冲突
2. **报错**: `cudnn_cnn64_9.dll` 加载失败，导致程序无法启动
3. **环境敏感**: 需要特定版本的 CUDA/cuDNN 组合，难以在普通用户环境部署
4. **模型体积大**: 首次使用需下载约300MB模型文件

**弃用决策**: v2.2版本完全移除 InsightFace 相关代码，避免环境问题

#### YuNet+SFace (OpenCV DNN)

| 项目 | 说明 |
|------|------|
| **检测器** | YuNet (轻量级CNN) |
| **识别器** | SFace (128维特征，余弦相似度) |
| **运行环境** | OpenCV DNN (纯CPU) |
| **模型格式** | ONNX |
| **模型体积** | 检测1.8MB + 识别37MB |

**选择原因**:
1. **轻量高效**: 纯CPU运行，速度快
2. **无依赖冲突**: 仅依赖 OpenCV，与现有环境完全兼容
3. **检测能力强**: 对侧脸、小脸有良好检测效果
4. **易于部署**: ONNX模型体积小，下载快

**集成过程**:
1. 参考同学的OpenCV实现方案
2. 创建 `core/opencv_dnn_engine.py` 独立引擎类
3. 处理中文路径问题（模型文件复制到临时目录）
4. 统一相似度显示（余弦相似度转0-100%）

### 1.3 相似度统一化

**问题**: 不同模型使用不同的距离/相似度度量，难以横向比较

| 模型 | 原始度量 | 范围 | 含义 |
|------|----------|------|------|
| HOG/CNN | 欧氏距离 | 0~1.2 | 越小越相似 |
| YuNet | 余弦相似度 | -1~1 | 越大越相似 |

**解决方案**: 在 `core/matcher.py` 中实现 `distance_to_similarity()` 函数

```python
def distance_to_similarity(distance, method):
    if method == 'euclidean':
        # 欧氏距离转相似度: distance=0 → 100%, distance=1 → 0%
        return (1.0 - distance) ** 0.8 * 100
    elif method == 'cosine_dist':
        # 余弦距离转相似度
        return (1.0 - distance) * 100
```

**效果**: 所有模型统一显示0-100%相似度，便于用户理解和比较

---

## 二、UI界面优化过程

### 2.1 界面演进路线

```
v1.0 基础界面
    └── 简单布局，功能按钮，基础样式
         │
v1.1~v1.2 功能增强
    └── 添加参数滑块、批量处理、结果保存
         │
v2.0 现代化改造
    ├── macOS风格样式 (macos.qss)
    ├── 可缩放图像组件 (ZoomableImageLabel)
    └── 多线程防卡顿
         │
v2.1 性能优化
    ├── GPU状态显示
    ├── 性能统计面板
    └── HTML报告导出
         │
v2.2 交互增强
    ├── 模型对比窗口
    ├── 可视化处理过程
    └── 缓存机制
         │
v3.0 Apple风格重构
    ├── modern.qss 全新样式
    ├── 卡片式布局 + 阴影效果
    ├── 状态药丸指示器
    ├── 拖拽上传
    ├── 长按对比原图
    └── 一键识别功能
```

### 2.2 关键UI优化详解

#### 2.2.1 样式系统演进

**v1.x - styles.qss (基础样式)**
- 简单的颜色定义
- 基础按钮、滑块样式
- 功能性为主

**v2.0 - macos.qss (macOS风格)**
- 浅灰背景色 (#F5F5F7)
- 圆角按钮和控件
- 蓝色强调色 (#007AFF)
- GroupBox分组样式

**v3.0 - modern.qss (Apple设计语言)**
- 全白卡片 + 柔和阴影
- 大圆角 (16px)
- 按钮按压效果（下沉动画）
- 滑块重新设计（圆形手柄）
- 状态药丸样式

```css
/* v3.0 卡片样式示例 */
QFrame#SidePanel {
    background-color: #FFFFFF;
    border-radius: 16px;
    border: 1px solid #E5E5EA;
}

/* v3.0 按钮按压效果 */
QPushButton:pressed {
    background-color: #E5E5EA;
    padding-top: 10px;    /* 模拟下沉 */
    padding-bottom: 6px;
}
```

#### 2.2.2 布局优化

**v1.x~v2.x 布局**:
```
┌────────────────────────────────────┐
│ [工具栏按钮...]                      │
├──────────┬─────────────────────────┤
│ 目标区域  │                         │
│          │      场景显示区域         │
│ 参数区域  │                         │
│          │                         │
├──────────┴─────────────────────────┤
│ [状态栏]                            │
└────────────────────────────────────┘
```

**v3.0 Apple风格布局**:
```
┌────────────────────────────────────────┐
│          [状态药丸]                     │
├──────────────┬─────────────────────────┤
│              │                         │
│  ┌────────┐  │                         │
│  │ 悬浮式  │  │     沉浸式预览区         │
│  │ 参数卡片 │  │                         │
│  │ (带阴影) │  │     (支持拖拽上传)       │
│  │         │  │     (长按对比原图)       │
│  └────────┘  │                         │
│              │                         │
└──────────────┴─────────────────────────┘
```

#### 2.2.3 交互增强

**拖拽上传 (v3.0)**
- 实现 `ImageDropLabel` 组件
- 支持直接拖拽图片到窗口
- 自动判断是目标还是场景

```python
class ImageDropLabel(QLabel):
    def dropEvent(self, event):
        file_path = event.mimeData().urls()[0].toLocalFile()
        # 智能判断加载类型
        if not self.engine.has_target:
            self.load_as_target(file_path)
        else:
            self.load_as_scene(file_path)
```

**长按对比 (v3.0)**
- 按住鼠标左键显示原图
- 松开显示识别结果
- 直观对比处理效果

```python
def mousePressEvent(self, event):
    if self.result_pixmap and self.original_pixmap:
        self.setPixmap(self.original_pixmap)  # 显示原图

def mouseReleaseEvent(self, event):
    if self.result_pixmap:
        self.setPixmap(self.result_pixmap)    # 恢复结果
```

**状态药丸 (v3.0)**
- 替代传统状态栏
- 颜色编码状态
  - 🟢 绿色: 成功
  - 🔵 蓝色: 处理中
  - 🔴 红色: 错误
  - ⚪ 灰色: 就绪

#### 2.2.4 功能窗口

**可视化处理过程 (v2.2)**
- 展示完整处理流程
- 各阶段图像对比
- 支持缩放查看

**模型对比窗口 (v2.2)**
- 同时运行多个模型
- 分段进度条显示
- 并排对比识别效果

**一键识别 (v3.0)**
- 批量导入后可重新识别
- 切换模型/参数后快速验证
- 显示总耗时统计

### 2.3 性能优化

| 优化项 | 版本 | 效果 |
|--------|------|------|
| 多线程处理 | v1.1 | UI不再卡顿 |
| 大图预缩放 | v2.2 | HOG速度提升50%+ |
| 可视化缓存 | v2.2 | 重复查看无需重算 |
| 批量总耗时 | v3.0 | 准确统计处理时间 |

---

## 三、版本发布记录

| 版本 | 日期 | 主要更新 |
|------|------|----------|
| v1.0 | 2024-12 | 基础人脸检测与识别 |
| v1.1 | 2024-12 | 多线程、批量处理 |
| v1.2 | 2024-12 | 参数调节、结果保存 |
| v2.0 | 2024-12 | macOS风格UI、可缩放图像 |
| v2.1 | 2024-12 | GPU支持、HTML报告、性能统计 |
| v2.2 | 2024-12 | YuNet引擎、模型对比、可视化 |
| v3.0 | 2024-12 | Apple风格重构、最终版 |

---

## 四、技术决策总结

### 4.1 成功的决策

1. **采用 face_recognition 库**: 封装良好，易于使用，精度可靠
2. **引入 YuNet 替代 InsightFace**: 解决环境冲突，保持轻量
3. **多线程架构**: 从一开始就避免UI卡顿问题
4. **统一相似度显示**: 提升用户体验和可比较性

### 4.2 经验教训

1. **InsightFace 集成失败**: 
   - 教训: 评估第三方库时要充分测试环境兼容性
   - 改进: 选择更轻量、依赖更少的方案

2. **中文路径问题**:
   - 教训: OpenCV对中文路径支持不佳
   - 改进: 实现临时文件复制机制

3. **性能优化时机**:
   - 教训: 早期未考虑大图处理性能
   - 改进: v2.2补充自适应缩放逻辑

---

## 五、最终技术栈

```
DeepFocus Pro v3.0
│
├── 核心依赖
│   ├── face-recognition >= 1.3.0
│   ├── opencv-python >= 4.5.4
│   ├── PyQt5 >= 5.15.0
│   ├── numpy >= 1.21.0
│   ├── dlib >= 19.22.0
│   └── jinja2 >= 3.0.0
│
├── 检测引擎
│   ├── HOG (dlib) - 快速CPU检测
│   ├── CNN (dlib) - 高精度GPU检测
│   └── YuNet (OpenCV DNN) - 轻量级检测
│
├── 识别引擎
│   ├── dlib 128D - HOG/CNN模式
│   └── SFace 128D - YuNet模式
│
└── 界面技术
    ├── PyQt5 - GUI框架
    ├── QSS - 样式系统
    └── QThread - 多线程
```

---

**报告完成时间**: 2024年12月  
**项目状态**: v3.0 最终版 ✅

